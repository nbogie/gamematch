<%= form_for(@player) do |f| %>
  <% if @player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
      <% @player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bgg_username %><br>
    <%= f.text_area :bgg_username %>
  </div>
  <div class="field">
    <%= f.label :bgg_user_id %><br>
    <%= f.text_area :bgg_user_id %>
  </div>
  <div class="field">
    <%= f.label :meetup_username %><br>
    <%= f.text_area :meetup_username %>
  </div>
  <div class="field">
    <%= f.label :meetup_user_id %><br>
    <%= f.number_field :meetup_user_id %>
  </div>
  <div class="field">
    <%= f.label :meetup_bio %><br>
    <%= f.text_area :meetup_bio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
